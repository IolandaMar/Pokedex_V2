name: pokedexv2
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^0.13.6
  provider: ^6.1.2
  shared_preferences: ^2.2.2

  # Si vols tornar a afegir notificacions locals més endavant:
  # flutter_local_notifications: ^14.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
      - assets/pokeball.png
